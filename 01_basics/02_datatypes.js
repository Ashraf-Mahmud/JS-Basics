"use strict"; //treat all java code as a newer version

//data types
let number=1324;
let string="String";
let boolean= true;
let Null=null; //null means empty value not 0. 0 is a value in many case
let Undefined; // undefined means value not assigned
//unique : to define unique value like in figma or react

// react 

console.log(typeof "hel");

//tc39.es/ecmascript   and mdn.org documentation        https://tc39.es/ecma262/
